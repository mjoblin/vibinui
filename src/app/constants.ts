export const API_REFRESH_INTERVAL = 60 * 60 * 24; // Refresh API data once per day.

export const labelBesideComponentStyle = {
    root: {
        display: "flex",
        gap: 7,
        alignItems: "center",
    },
};